CREATE TABLE IF NOT EXISTS  core.bpms_bem_business_event (bpms_bem_business_event_sid  STRING COMMENT 'Surrgate Key', bpms_bem_business_event_id  STRING COMMENT 'Foreign Key', business_event_id  STRING, dtype  STRING, completion_timestamp  TIMESTAMP, date_insert  TIMESTAMP, date_update  TIMESTAMP, due_date  TIMESTAMP, gevo_id  STRING, init_timestamp  TIMESTAMP, originator_bp_id  STRING, revocation_deadline  TIMESTAMP, status_details  STRING, version  INTEGER, e2e_process_id  STRING, input_chanel_id  INTEGER, be_reason_status_id  INTEGER, business_event_status_id  INTEGER, business_event_type_id  INTEGER, dwh_counter  INTEGER, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(business_event_id) disable novalidate ) PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');