CREATE TABLE IF NOT EXISTS  core.bpms_bem_input_document (bpms_bem_input_document_sid  STRING COMMENT 'Surrgate Key', bpms_bem_package_entry_id  STRING COMMENT 'Foreign Key', bpms_document_id  STRING COMMENT 'Foreign Key', general_document_id  STRING, ims_id  STRING, processing_timestamp  TIMESTAMP, receive_timestamp  TIMESTAMP, returned_document_id  STRING, scan_timestamp  TIMESTAMP, sender_id  STRING, envelope_id  STRING, input_chanel_id  INTEGER, input_document_type_id  INTEGER, dwh_counter  INTEGER, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(general_document_id) disable novalidate ) PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');