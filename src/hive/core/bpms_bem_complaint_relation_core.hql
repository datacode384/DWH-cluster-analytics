CREATE TABLE IF NOT EXISTS  core.bpms_bem_complaint_relation (bpms_bem_complaint_relation_sid  STRING COMMENT 'Surrgate Key', bpms_bem_complaint_relation_id  STRING COMMENT 'Foreign Key', general_relation_id  STRING, relation_type_id  INTEGER, source_id  STRING, target_id  STRING, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(general_relation_id) disable novalidate ) PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');