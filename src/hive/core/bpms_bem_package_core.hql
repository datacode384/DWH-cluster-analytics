CREATE TABLE IF NOT EXISTS  core.bpms_bem_package (bpms_bem_package_sid  STRING COMMENT 'Surrgate Key', bem_generic_id  STRING COMMENT 'Foreign Key', bpms_bem_package_id  STRING COMMENT 'Foreign Key', package_id  STRING, completion_timestamp  TIMESTAMP, creation_timestamp  TIMESTAMP, date_insert  TIMESTAMP, date_update  TIMESTAMP, dwh_counter  INTEGER, external_package_correlation_id  STRING, fetch_timestamp  TIMESTAMP, receive_timestamp  TIMESTAMP, send_timestamp  TIMESTAMP, stack_id  STRING, version  INTEGER, package_status_id  INTEGER, package_type_id  INTEGER, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(package_id, external_package_correlation_id) disable novalidate ) PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');