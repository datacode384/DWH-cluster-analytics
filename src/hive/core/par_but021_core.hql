CREATE TABLE IF NOT EXISTS  core.par_but021 (diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', par_but021_sid  STRING COMMENT 'Surrgate Key', record_hash  STRING COMMENT 'Hash for all non technical attributes.', source_system  INTEGER COMMENT 'Name of source system.', wirksam_ab  TIMESTAMP COMMENT '', z_cre_dida  INTEGER COMMENT 'Timestamp initial import DiDa', z_ini_tims  INTEGER COMMENT 'Timestamp initial transfer', z_logsys_src  INTEGER COMMENT 'Source', z_upd_dida  INTEGER COMMENT 'Timestamp update import DiDa', z_upd_tims  INTEGER COMMENT 'Timestamp last updated transfer', par_but000_id  STRING COMMENT 'Foreign Key', par_but021_id  STRING COMMENT 'Foreign Key', partner  STRING COMMENT 'Geschäftspartnernummer', adr_kind  STRING COMMENT 'Adressart', addrnumber  STRING COMMENT 'Adressnummer', xdfadu  STRING COMMENT 'Kennzeichen: Standardadresse einer Adressverwendung', primary key(partner, adr_kind) disable novalidate ) PARTITIONED BY (client  STRING COMMENT 'Mandant', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');