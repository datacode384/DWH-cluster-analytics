CREATE TABLE IF NOT EXISTS  core.pas_ktostd (pas_ktostd_sid  STRING COMMENT 'Surrgate Key', pas_ktostd_id  STRING COMMENT 'Foreign Key', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbid  INTEGER COMMENT 'the historical version number (logId)', ktostdtypid  STRING COMMENT 'id of KtoStd (Eindeutige Identifizierung eines Kontostand-Typs.)', saldo  DECIMAL(22,8) COMMENT 'Saldo des entsprechenden Kontos zu einem bestimmten Zeitpunkt.', vtid  SMALLINT COMMENT 'id of KtoStd (Das Field vtId identifiziert zusammen mit dem Field lvId denVertragsteil, zu', vbid  SMALLINT COMMENT 'id of KtoStd (Nummer zur eindeutigen Identifizierung eines Vertragsbausteins innerhalb ein', waehrungid  INTEGER COMMENT 'Das Attribut dient zur eindeutigen Identifizierung einer Waehrung', fondsid  INTEGER COMMENT 'id of KtoStd (Interne Kennummer eines Fonds)', portfid  INTEGER COMMENT 'id of KtoStd (Interne Kennnummer eines Portfolios)', tpa_mandant STRING, wirksam_ab TIMESTAMP, gueltig_ab TIMESTAMP, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(lvid, ktostdtypid, vtid, vbid, fondsid, portfid) disable novalidate ) COMMENT 'Kontostand' PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');