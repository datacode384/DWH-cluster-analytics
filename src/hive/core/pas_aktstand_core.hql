CREATE TABLE IF NOT EXISTS  core.pas_aktstand (pas_aktstand_sid  STRING COMMENT 'Surrgate Key', pas_lv_id  STRING COMMENT 'Foreign Key', pas_sr_id  STRING COMMENT 'Foreign Key', lvid  STRING, bearbidlast  INTEGER, vtidlast  SMALLINT, vbidlast  SMALLINT, genlast  SMALLINT COMMENT 'Letzte vergebene Generationsnummer am VB', sbidlast  INTEGER COMMENT 'Letzte vergebene Kennzeichnung einer Sammelbewegung (=sbId)', ktobewidlast  INTEGER COMMENT 'Nummer der letzten durchgefuehrten Kontobewegung', zahlempfid  INTEGER COMMENT 'Nummer des zuletzt erzeugten Zahlungsempfaengers', beguenstid  SMALLINT, ktobelegidlast  INTEGER COMMENT 'ktoBelegIdLast', sntnwidlast  INTEGER COMMENT 'Letzte vergebene Identifikation fuer Schnittstellennachweise', mahnidlast  INTEGER COMMENT 'Die ID der zuletzt erzeugten Mahnung in einem Vertrag.', doknwidlast  INTEGER, drittidlast  INTEGER, eaklauselidlast  INTEGER COMMENT 'Die letzte vergebene laufende Nummer in eaKlausel (lfdnr).', schwebeidlast  INTEGER COMMENT 'Die letzte vergebene Identifikation fuer Schwebenachweise.', hvgruppeidlast  SMALLINT, regidlast  INTEGER COMMENT 'Die letzte vergebene laufende Nummer in steuAuftReg', kindidlast  INTEGER COMMENT 'Die letzte vergebene kindId', schichtidlast  INTEGER COMMENT 'Die letzte vergebene schichtId', regidbtglast  INTEGER COMMENT 'Die letzte vergebene regIdBtgLast', tabselbv  BIGINT COMMENT 'Performanceoptimierung: In der Tabelle AKTSTAND wird in einem Bitfeld die Informationen ge', prvidlast  INTEGER COMMENT 'Die letzte vergebene laufende Nummer in prv', teilrentenidlast  INTEGER COMMENT 'Die letzte vergebene laufende Nummer in SteuWerteRenteAuft', tpa_mandant STRING, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(lvid) disable novalidate ) PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');