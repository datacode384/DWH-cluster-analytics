CREATE TABLE IF NOT EXISTS  core.pas_sntvtlstg (pas_sntvtlstg_sid  STRING COMMENT 'Surrgate Key', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', pas_sntvtlstg_id  STRING COMMENT 'Foreign Key', pas_vt_id  STRING COMMENT 'Foreign Key', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', bearbid  INTEGER COMMENT 'id of SntVtLstg (Das Field bearbID identifiziert den Vertragsstand (und damit gleichzeitig', bearbidref  INTEGER COMMENT 'id of SntVtLstg (Das Field bearbIdRef identifiziert zusammen mit dem Field lvId den Vertra', lstgtyp  INTEGER COMMENT 'id of SntVtLstg (Leistungsart)', lstgwertakt  DECIMAL(18,4) COMMENT 'Aktueller Leistungswert', tpa_mandant STRING, wirksam_ab TIMESTAMP, gueltig_ab TIMESTAMP, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(lvid, vtid, lstgtyp) disable novalidate ) COMMENT 'Speicherung von Leistungen je Leistungsart am Vertragsteil' PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');