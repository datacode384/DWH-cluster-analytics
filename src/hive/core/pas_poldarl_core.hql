CREATE TABLE IF NOT EXISTS  core.pas_poldarl (pas_poldarl_sid  STRING COMMENT 'Surrgate Key', pas_lv_id  STRING COMMENT 'Foreign Key', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', mandantid  INTEGER COMMENT 'Das Field mandantId enthaelt einen Schluesselwert fuer den Mandant, fuer den diese Row gue', zahlwegpoldarl  INTEGER COMMENT 'Zahlweg Policendarlehen', zahlwegzins  INTEGER COMMENT 'Zahlweg Zinszahler Policendarlehen', zahlwegrueckz  INTEGER COMMENT 'Zahlweg fuer die Rueckzahlung des Policendarlehens', zinssatz  DECIMAL(7,3) COMMENT 'Prozentsatz; wird bestimmt auf globpara', abwzinssatz  DECIMAL(7,3) COMMENT 'Abweichender Zins (Prozentsatz)', zinsab  TIMESTAMP COMMENT 'Zeitpunkt ab dem das Policendarlehen zu verzinsen ist', zweizins  INTEGER COMMENT 'Vereinbarte Zahlweise der Zinsen fuer das Policendarlehen', tpa_mandant STRING, wirksam_ab TIMESTAMP, gueltig_ab TIMESTAMP, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(lvid) disable novalidate ) COMMENT 'Policendarlehen' PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');