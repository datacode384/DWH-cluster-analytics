CREATE TABLE IF NOT EXISTS  core.bpms_bem_processing_goal (bpms_bem_processing_goal_sid  STRING COMMENT 'Surrgate Key', bpms_bem_e2e_process_timeslot_id  STRING COMMENT 'Foreign Key', bpms_bem_processing_goal_id  STRING COMMENT 'Foreign Key', processing_goal_id  STRING, date_insert  TIMESTAMP, date_update  TIMESTAMP, originator  STRING, version  INTEGER, e2e_process_timeslot_id  STRING, general_process_step_id  STRING, processing_goal_status_id  INTEGER, processing_goal_type_id  INTEGER, dwh_counter  INTEGER, creation_timestamp  TIMESTAMP, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(processing_goal_id) disable novalidate ) PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');