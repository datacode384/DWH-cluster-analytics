CREATE TABLE IF NOT EXISTS  core.bpms_bem_complaint (bpms_bem_complaint_sid  STRING COMMENT 'Surrgate Key', bpms_bem_business_event_id  STRING COMMENT 'Foreign Key', bpms_bem_complaint_id  STRING COMMENT 'Foreign Key', complaint_id  STRING, complaint_external_reference  STRING, complaint_init_timestamp  TIMESTAMP, complaint_note  STRING, complaint_success  SMALLINT, complaint_top_flag  SMALLINT, complaint_value  STRING, date_insert  TIMESTAMP, date_update  TIMESTAMP, version  INTEGER, business_event_id  STRING, complaint_initiator_type_id  INTEGER, complaint_owner_type_id  INTEGER, reason_details_type_id  INTEGER, reason_type_id  INTEGER, complaint_status_id  INTEGER, success_evaluation_type_id  INTEGER, complaint_type_id  INTEGER, complaint_variant_type_id  INTEGER, complaint_hcm_type_id  INTEGER, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(complaint_id) disable novalidate ) PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');