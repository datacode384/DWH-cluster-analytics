CREATE TABLE IF NOT EXISTS  core.bpms_bem_package_entry (bpms_bem_package_entry_sid  STRING COMMENT 'Surrgate Key', bpms_bem_package_entry_id  STRING COMMENT 'Foreign Key', bpms_bem_package_id  STRING COMMENT 'Foreign Key', package_entry_id  STRING, dtype  STRING, date_insert  TIMESTAMP, date_update  TIMESTAMP, dwh_counter  INTEGER, external_correlation_id  STRING, version  INTEGER, package_id  STRING, package_entry_status_id  INTEGER, package_entry_type_id  INTEGER, diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(package_entry_id, external_correlation_id) disable novalidate ) PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');