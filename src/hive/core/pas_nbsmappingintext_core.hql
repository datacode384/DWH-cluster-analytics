CREATE TABLE IF NOT EXISTS  core.pas_nbsmappingintext (pas_nbsmappingintext_sid  STRING COMMENT 'Surrgate Key', id_type  INTEGER COMMENT 'id of NbsMappingIntExt (Gibt den Typ an, fuer den das Attribut ID_INT das letzte vergebene', id_int  INTEGER COMMENT 'id of NbsMappingIntExt (Interne numerische ID (z.B. partnerID usw.))', pas_nbsmappingintext_id  STRING COMMENT 'Foreign Key', mandantid  INTEGER COMMENT 'Das Field mandantID enthaelt einen Schluesselwert fuer den Mandant, fuer den diese Row gue', id_ext  STRING COMMENT 'Externe alpha-numerische ID', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(id_type, id_int) disable novalidate ) COMMENT 'Mappingtabelle interne zu externer ID und vv.' PARTITIONED BY (process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet TBLPROPERTIES ('PARQUET.COMPRESS'='SNAPPY');