CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (antrabw_sid  STRING COMMENT 'Surrgate Key', antrabw_id  STRING COMMENT 'Foreign Key Key table', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', antrabwid  INTEGER COMMENT 'id of AntrAbw (Intern vergebene Nummer, der die Antragsabweichung innerhalb der LV identif', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', antrabwart  INTEGER COMMENT 'Art der Abweichung vom Antrag', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(antrabwid, bearbidabg, lvid) disable novalidate ) COMMENT 'antrAbw' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;