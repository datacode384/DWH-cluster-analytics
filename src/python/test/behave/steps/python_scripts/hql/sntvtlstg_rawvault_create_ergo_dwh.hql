CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', bearbid  INTEGER COMMENT 'id of SntVtLstg (Das Field bearbID identifiziert den Vertragsstand (und damit gleichzeitig', bearbidref  INTEGER COMMENT 'id of SntVtLstg (Das Field bearbIdRef identifiziert zusammen mit dem Field lvId den Vertra', lstgtyp  INTEGER COMMENT 'id of SntVtLstg (Leistungsart)', lstgwertakt  DECIMAL(4,4) COMMENT 'Aktueller Leistungswert', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'Speicherung von Leistungen je Leistungsart am Vertragsteil' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;