CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (id_type  INTEGER COMMENT 'id of NbsMappingIntExt (Gibt den Typ an, fuer den das Attribut ID_INT das letzte vergebene', id_int  INTEGER COMMENT 'id of NbsMappingIntExt (Interne numerische ID (z.B. partnerID usw.))', mandantid  INTEGER COMMENT 'Das Field mandantID enthaelt einen Schluesselwert fuer den Mandant, fuer den diese Row gue', id_ext  STRING COMMENT 'Externe alpha-numerische ID', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'Mappingtabelle interne zu externer ID und vv.' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;