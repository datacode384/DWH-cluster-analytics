CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (sntvtlstg_sid  STRING COMMENT 'Surrgate Key', sntvtlstg_id  STRING COMMENT 'Foreign Key Key table', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', vt_sid  STRING COMMENT 'Foreign Key', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', vt_id  STRING COMMENT 'Foreign Key', bearbid  INTEGER COMMENT 'id of SntVtLstg (Das Field bearbID identifiziert den Vertragsstand (und damit gleichzeitig', bearbidref  INTEGER COMMENT 'id of SntVtLstg (Das Field bearbIdRef identifiziert zusammen mit dem Field lvId den Vertra', lstgtyp  INTEGER COMMENT 'id of SntVtLstg (Leistungsart)', lstgwertakt  DECIMAL(4,4) COMMENT 'Aktueller Leistungswert', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbid, bearbidref, lstgtyp, lvid, vtid) disable novalidate ) COMMENT 'Speicherung von Leistungen je Leistungsart am Vertragsteil' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;