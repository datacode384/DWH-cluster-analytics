CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (partnerid  INTEGER COMMENT 'id of PartnerLV (Das Field partnerId identifiziert denPartner, zu dem diese Row eine Aussa', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', drittid  INTEGER COMMENT 'id of Drittrecht (generically derived from Drittrecht)', bearbid  INTEGER COMMENT 'the historical version number (logId)', drittrechtid  INTEGER COMMENT 'Identifizierung eines zulaessiges Drittrechts', beginn  TIMESTAMP COMMENT 'Termin, ab dem die Vereinbarung wirksam wird.', drittrechtbez  STRING COMMENT 'Referenz, auf die sich das Drittrecht bezieht, z.B. Aktenzeichen, Darlehensnummer oder Gru', drittrechterl  DECIMAL(2,2) COMMENT 'Betrag der Drittberechtigung beim Erleben.', drittrechttod  DECIMAL(2,2) COMMENT 'Betrag der Drittberechtigung im Todesfall.', rang  INTEGER COMMENT 'Rangfolge des Drittrechtes, eindeutig innerhalb eines Vertrages.', drittrechtartid  INTEGER COMMENT 'Art des Drittrechts', kzstschaedzession  INTEGER COMMENT 'Kennzeichen, ob eine steuerschaedliche Zession vorliegt', rollenid  INTEGER COMMENT 'id of PartnerLV (Identifikation einer Partnerrolle in einer Partner-Vertrags-Beziehung)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', abwhoehe  DECIMAL(2,2) COMMENT 'Abweichende Hoehe der Drittrechtsumme', c_klassifiz  INTEGER COMMENT 'Schluesselfeld zur Klassifizierung der Drittrechtart', c_pfaendbesdat  TIMESTAMP COMMENT 'Datumsfeld zur Erfassung des Pfaendungsbeschlussdatums', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'Die Rows in Table drittrecht repraesentieren berechtigte Interessen Drittberechtigter (z.B. Vormund, Pfandglaeubiger, ...) an einer Leistung aus dem Vertrag. Die Wirksamkeit eines Drittrechts ist abhaengig von der Anzeige gegenueber dem VU. Waehrend der ' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;