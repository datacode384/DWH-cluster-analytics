CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', bearbid  INTEGER COMMENT 'id of SntVt (Das Field bearbID identifiziert den Vertragsstand (und damit gleichzeitig gen', bearbidref  INTEGER COMMENT 'id of SntVt (Das Field bearbIdRef identifiziert zusammen mit dem Field lvId den Vertragsst', tfid  STRING COMMENT 'Das Field identifiziert denTarif, der dem durch bearbIdRef identifiziertenVertragsteilstan', zb  DECIMAL(2,2) COMMENT 'Der je Ratenzahlungsabschnitt (tariflich) faellige Zahlbeitrag. Eine Beitragsreduktion aus', la1  DECIMAL(4,4) COMMENT 'In der Tarifklasse Klassik-Kapital oder Risiko ist la1 die (anfaengliche) Todesfallsumme, ', la2  DECIMAL(2,2) COMMENT 'In der Tarifklasse Klassik-Kapital ist la2 die (abweichende) Erlebensfallsumme, bei Rente ', res  DECIMAL(2,2) COMMENT 'In der anlageorientierten Versicherungsmathematik gibt es ""die Reserve"" res, in der klas', rkw  DECIMAL(2,2) COMMENT 'Rueckkaufswert - z.B. pro Hauptversicherungsteil in den Garantiewerten', akt_leist  DECIMAL(2,2) COMMENT 'Aktuelle Versicherungssumme.', rbeg  TIMESTAMP COMMENT 'Rentenbeginntermin', beginnvt  TIMESTAMP COMMENT 'Beginn des Vertragsteils (Termin)', vtabl  TIMESTAMP COMMENT 'Datum, zu dem der LV-Vertragsteil ablaeuft', bzabl  TIMESTAMP COMMENT 'Zu diesem Datum endet die planmaessige Beitragszahlung fuer den LV-Vertragsteil. Der Ablau', labl  TIMESTAMP COMMENT 'Ablauftermin der Leistung bei BUZ / HZV', gabl  TIMESTAMP COMMENT 'Ablauftermin der Garantiezeit', optabl  TIMESTAMP COMMENT 'Termin, an dem die flexible Ablaufphase endet.', waehrungid  INTEGER COMMENT 'Das Attribut dient zur eindeutigen Identifizierung einer Waehrung', schrittid  INTEGER COMMENT 'Die schrittId desArbeitsschritts, durch den der Vertragsstand bearbIdRef entstand.', akla  DECIMAL(2,2) COMMENT 'Abschlusskosten, die bei beitragspflichtigenVertragsteilstaendenim abgelaufenen Kalenderja', inka  DECIMAL(2,2) COMMENT 'Inkassokosten (inklusive Sammelinkassorabatt), die bei beitragspflichtigen Vertragsteilsta', rb1  DECIMAL(2,2) COMMENT 'Das Field rb1 enthaelt jenen Betrag, der im abgelaufenen Kalenderjahr zur Abdeckung des To', rzres  DECIMAL(2,2) COMMENT 'Das Field rzres enthaelt sie Summe der rechnungsmaessigen Zinsen auf das Deckungskapital, ', vwkdkr  DECIMAL(2,2) COMMENT 'Verwaltungskosten, die im Rahmen der Beitragszerlegung im abgelaufenen Kalenderjahr aus de', koueb  DECIMAL(2,2) COMMENT 'Kostenueberschuss, der im Rahmen der anlageorientierten ueberschussverteilung im abgelaufe', riueb  DECIMAL(2,2) COMMENT 'Risikoueberschuss, der im Rahmen der anlageorientierten ueberschussverteilung im abgelaufe', zinsueb  DECIMAL(2,2) COMMENT 'Die Summe der im abgelaufenen Kalenderjahr anfallenden ueberschuesse auf rechnungsmaessige', fvmueb  DECIMAL(2,2) COMMENT 'Fondsvermoegen aus ueberschuessen zum Zeitpunkt bearbIdRef.', verans  DECIMAL(2,2) COMMENT 'Erreichter Stand des Ansammlungsguthabens zum Zeitpunkt bearbIdRef. Siehe auch ColumnskBil', bonres  DECIMAL(2,2) COMMENT 'Bonusreserve zum Zeitpunkt bearbIdRef. Siehe auch Column sntVt.res.', basres  DECIMAL(2,2) COMMENT 'Basisreserve zum Zeitpunkt bearbIdRef. Siehe auch Column sntVt.res.', leistbon  DECIMAL(2,2) COMMENT 'Bonusleistung', minbtg  DECIMAL(2,2) COMMENT 'Vereinbarter Mindestbeitrag', gebuehr  DECIMAL(2,2) COMMENT 'Angefallene Gebuehren', jahr  SMALLINT COMMENT 'Das Kalenderjahr, fuer welches die Summe der Zulagen im Field zulSum ausgewiesen ist.', btgsumjahr  DECIMAL(2,2) COMMENT 'Die Summe der Beitraege im Bezugsjahr.', zulsum  DECIMAL(2,2) COMMENT 'Die Summe der Zulagen des Vertragsteils im Kalenderjahr, das durch das Field jahr bestimmt', zulsumjahr  DECIMAL(2,2) COMMENT 'Das Field zulSumJahr enthaelt die in einem Kalenderjahr gutgeschriebenen Riester-Zulagen; ', btgsum  DECIMAL(2,2) COMMENT 'Die absolute Summe der Beitraege.', lstgef  DECIMAL(2,2) COMMENT 'Leistung aus gefoerderten Beitraegen.', btggef  DECIMAL(2,2) COMMENT 'gefoerderte Beitraege', fondsertrag  DECIMAL(2,2) COMMENT 'fondsErtrag', uebverr  DECIMAL(2,2) COMMENT 'Beitragsverrechnung / Barauszahlung', suetod  DECIMAL(2,2) COMMENT 'Leistung aus Schlussueberschuss bei Tod', rkwsue  DECIMAL(2,2) COMMENT 'Leistung aus Schlussueberschuss bei Rueckkauf', lstsgw  DECIMAL(2,2) COMMENT 'Leistung aus Schlussueberschuessen. Die Leistungen aus Schlussueberschuss werden auf Vertr', kuebr  DECIMAL(2,2) COMMENT 'Differenz zwischen konstanter Ueberschussrente und bereits zugeteilter Bonusrente', ent_dm  DECIMAL(2,2) COMMENT 'Betrag (in Landeswaehrung ) bei Switchen und bei Entnahme.', uebverwsys  INTEGER COMMENT 'Eindeutige Identifizierung eines Ueberschussverwendungssystems.', stornoabschlag  DECIMAL(2,2) COMMENT 'Stornoabschlag beim Rueckkauf', lstuebbr  DECIMAL(2,2) COMMENT 'Leistung aus Bewertungsreserven', bezuguebbr  DECIMAL(2,2) COMMENT 'fuer die Leistungsbearbeitung relevante Bewertungsreserve (Groesse aus BewertReserve)', basisuebbr  DECIMAL(2,2) COMMENT 'fuer die Leistungsbearbeitung relevante kollektive Basisgroesse (Groesse aus BestandsUebBR', lstges  DECIMAL(2,2) COMMENT 'Gesamtleistung', ertrges  DECIMAL(2,2) COMMENT 'Gesamtertrag/Kapitalertrag', ertrgef  DECIMAL(2,2) COMMENT 'Ertrag aus gefoerderten Beitraegen.', fvmwertsicher  DECIMAL(2,2) COMMENT 'Wert der Anteile der wertgesicherten Anlage', lstuebbrtod  DECIMAL(2,2) COMMENT 'Leistung aus Bewertungsreserven bei Tod', ertrngef  DECIMAL(2,2) COMMENT 'nicht gefoerderter Ertrag', akt_rent  DECIMAL(2,2) COMMENT 'Rente (ohne Rente aus Ueberschussbeteiligung)', akt_rent_ues  DECIMAL(2,2) COMMENT 'Rente aus Ueberschussbeteiligung', garguthabbe  DECIMAL(2,2) COMMENT 'erreichtes garantiertes Guthaben gemaess Beitragserhalt', garguthabge  DECIMAL(2,2) COMMENT 'erreichtes garantiertes Guthaben gemaess Guthabenerhalt', la3  DECIMAL(2,2) COMMENT 'Enthaelt bei laufenden DH-Renten die maximal garantierbare Rente, ansonsten leer', garniveaurente  DECIMAL(6,6) COMMENT 'Niveau der Rentengarantie', kzrente  SMALLINT COMMENT 'Art der Verrentung', guth_ues_unverw  DECIMAL(2,2) COMMENT 'Guthaben noch nicht verwendeter Ueberschuesse', larbegges  DECIMAL(2,2) COMMENT 'Gesamtrente bei Rentenbeginn - fuer Pruefung auf Kleinbetragsrente', beivorrat  DECIMAL(2,2) COMMENT 'Beitragsuebertrag im Bezugsjahr. Dieser enthaelt die Summe derjenigen im Bezugsjahr gezahl', sueres  DECIMAL(2,2) COMMENT 'Schlussueberschussreserve im Bezugsjahr', zulkorrjahr  DECIMAL(2,2) COMMENT 'Entnahme bei Zulagenkorrektur im Bezugsjahr', zuzsumjahr  DECIMAL(2,2) COMMENT 'Summe der Zuzahlungen im Bezugsjahr', anrkapjahr  DECIMAL(2,2) COMMENT 'Kapital aus Anbieterwechsel im Bezugsjahr', anzi  DECIMAL(2,2) COMMENT 'Zinsen auf verzinsliche Ansammlung im Bezugsjahr', inkavorrat  DECIMAL(2,2) COMMENT 'Inkassokostenuebertrag im Bezugsjahr. Dieser enthaelt den Anteil der bereits entnommenen I', sueresbewjahr  DECIMAL(2,2) COMMENT 'Veraenderung der Schlussueberschussreserve im Bezugsjahr', fvm  DECIMAL(2,2) COMMENT 'Wert (in Landeswaehrung) der Fondsanteile', rensumjahr  DECIMAL(2,2) COMMENT 'Summe der planmaessigen Rentenzahlungen im Bezugsjahr', lstuebbrbewjahr  DECIMAL(2,2) COMMENT 'Veraenderung der Leistung aus Ueberschuessen aus Bewertungsreserven im Bezugsjahr', lstuebbrmin  DECIMAL(2,2) COMMENT 'Leistung aus Mindestbeteiligung an Bewertungsreserven', lstuebbrmintod  DECIMAL(2,2) COMMENT 'Leistung aus Mindestbeteiligung an Bewertungsreserven bei Tod', ablaufalterjahre  INTEGER COMMENT 'Alter in Jahren der versicherten Person zum planmaessigen Ablauf einer Rente', ablaufaltermonate  INTEGER COMMENT 'Alter in Monaten der versicherten Person zum planmaessigen Ablauf einer Rente', uebverrsumjahr  DECIMAL(2,2) COMMENT 'Summe der Beitragsverrechnungen im Bezugsjahr', fondskickbackjahr  DECIMAL(2,2) COMMENT 'Fondskickbacks im Rahmen der laufenden (Kosten-) Ueberschussbeteiligung im Bezugsjahr', fondskostenjahr  DECIMAL(2,2) COMMENT 'Ausweisung der externen Fondskosten des abgelaufenen Kalenderjahres kumuliert ueber alle F', renueb  DECIMAL(2,2) COMMENT 'Rentenuebertrag', forderungakoffen  DECIMAL(2,2) COMMENT 'Forderungsbetrag fuer nicht getilgte Abschlusskosten bei Uebertragung', steuzuschlag  DECIMAL(2,2) COMMENT 'Steuerzuschlag', steuzuschlagsumjahr  DECIMAL(2,2) COMMENT 'Summe der Steuerzuschlaege im Bezugsjahr', ertrngefkorr  DECIMAL(2,2) COMMENT 'Ungefoerderter Ertrag, der nachtaeglich zu bescheinigen ist. Der Ausweis betrifft nur Teil', lstgefkorr  DECIMAL(2,2) COMMENT 'Gefoerderte Leistung, die nachtaeglich zu bescheinigen ist. Der Ausweis betrifft nur Teilr', wigutko  DECIMAL(2,2) COMMENT 'Das Field wigutko enthaelt die Summe der Kosten, die im abgelaufenen Kalenderjahr wiedergu', rentfaktgar  DECIMAL(8,8) COMMENT 'garantierter Rentenfaktor, d.h. die garantierte Rente je 10000 Euro Vermoegen zu Rentenbeg', lsterlgar  DECIMAL(2,2) COMMENT 'garantierte Erlebensfallleistung bei Ablauf', rentbetrgarakt  DECIMAL(2,2) COMMENT 'Garantierte Rente zum Zeitpunkt der aktuellen Berechnung.', fvmsuea  DECIMAL(2,2) COMMENT 'Fondsvermoegen aus Schlussueberschuessen', akt_rent_bewres  DECIMAL(2,2) COMMENT 'akt_rent_bewres', nachsteuer  DECIMAL(2,2) COMMENT 'Steuerbetrag aus der Nachversteuerung von Praemien', uebverrsum  DECIMAL(2,2) COMMENT 'Summe der Beitragsverrechnungen im gesamten Leben des Vertragsteils', zuzsum  DECIMAL(2,2) COMMENT 'Summe der Zuzahlungen im gesamten Leben des Vertragsteils', rensum  DECIMAL(2,2) COMMENT 'Summe der planmaessigen Rentenzahlungen im gesamten Leben des Vertragsteils', zulkorrsum  DECIMAL(2,2) COMMENT 'Summe der Entnahmen bei Zulagenkorrekturen im gesamten Leben des Vertragsteils', btgsonstsum  DECIMAL(2,2) COMMENT 'Summe der sonstigen Beitraege (z.B. Anbieterwechsel) im gesamten Leben des Vertragsteils', lstvertod  DECIMAL(2,2) COMMENT 'vereinbarte Leistung (ohne Ueberschuesse) bei Tod', lstuestod  DECIMAL(2,2) COMMENT 'Leistung aus laufender Ueberschussbeteiligung bei Tod', lstues  DECIMAL(2,2) COMMENT 'Leistung aus laufender Ueberschussbeteiligung bei Eintritt des primaer versicherten Risiko', btgsonstsumjahr  DECIMAL(2,2) COMMENT 'Summe der sonstigen Beitraege (z.B. Anbieterwechsel) im Bezugsjahr', aus_auf  DECIMAL(2,2) COMMENT 'Summe der Ausgabeaufschlaege, die im Bezugsjahr erhoben oder zurueckerstattet wurden', lstverunftod  DECIMAL(2,2) COMMENT 'vereinbarte Leistung (ohne Ueberschuesse) bei Unfalltod', lstuesunftod  DECIMAL(2,2) COMMENT 'Leistung aus laufender Ueberschussbeteiligung bei Unfalltod', bfrsum  DECIMAL(2,2) COMMENT 'beitragsfreie Summe', guthaendsonst  DECIMAL(2,2) COMMENT 'Sonstige Guthabenaenderung im Rahmen der jaehrlichen Kundeninformation', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'Die Rows in Table sntVt repraesentieren ausgewaehlte Vertragsteileigenschaften nach bestimmten. Die folgenden Non-Key-Columns sind immer besetzt:* tfId* bearbIdRef* schrittIdAndere Columns sind nur nach bestimmtenbesetzt. Der Ausdruck ""das abgelaufene K' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;