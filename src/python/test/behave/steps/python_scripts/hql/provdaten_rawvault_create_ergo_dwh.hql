CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', abagenturid  STRING COMMENT 'Nummer der Abschluss-Agentur', beagenturid  STRING COMMENT 'Nummer der Bestands-Agentur', miagenturid  STRING COMMENT 'Nummer der mitwirkenden Agentur', kzmanprov  INTEGER COMMENT 'Kennzeichen, ob die Provision manuell zu vergeben oder zu veraendern ist', kzvertraktion  INTEGER COMMENT 'Kennzeichen fuer Vertriebsaktionen.', kzwettbew  INTEGER COMMENT 'Kennzeichen fuer Vertriebswettbewerbe', bewsfakt  DECIMAL(8,8) COMMENT 'Bewertungsfaktor bei der Provisionierung', provkuerz  DECIMAL(8,8) COMMENT 'Faktor zur Kuerzung der Provision', kzprovdyn  INTEGER COMMENT 'Gibt an, wie im Falle eines Vermittler-Wechsels die Provision aus Dynamik aufgeteilt wird.', kzstores  INTEGER COMMENT 'Gibt Besonderheiten bei der Behandlung der Stornoreserve an.', kzmarket  INTEGER COMMENT 'Enthaelt eine Kennzeichnung des Marketings.', vertriebsweg  INTEGER COMMENT 'Vertriebsweg', maxzinsmod  DECIMAL(8,8) COMMENT 'maximaler Zinssatz fuer die Modellrechnung, vertriebswegabhaengig', zahlwegqual  INTEGER COMMENT 'Naehere Qualifikation des Zahlwegs', provart  INTEGER COMMENT 'Art des Provisionssatzes (Schluesselverzeichnis)1 - Abschlussprovision2 - Bestandsprovisio', kzberatprot  INTEGER COMMENT 'Kennzeichen Beratungsprotokoll', kzbako  INTEGER COMMENT 'Kennzeichen BAKO', kzlabel  INTEGER COMMENT 'Auspraegung: WL - Wuertt. Leben; KLV - Karlsruher Leben; Sonst - sonstige Altvertraege.', bearbstdort  INTEGER COMMENT 'Bearbeitungsstandort', beratprotpflicht  INTEGER COMMENT 'Beratungsprotokoll', partkey  SMALLINT COMMENT 'the partition key', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'Fuehrung historisch relevanter Provisionsdaten am Vertrag' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;