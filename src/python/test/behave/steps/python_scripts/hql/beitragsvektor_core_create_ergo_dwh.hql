CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (beitragsvektor_sid  STRING COMMENT 'Surrgate Key', beitragsvektor_id  STRING COMMENT 'Foreign Key Key table', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', vt_sid  STRING COMMENT 'Foreign Key', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', vt_id  STRING COMMENT 'Foreign Key', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', jahr  SMALLINT COMMENT 'id of Beitragsvektor (Angabe einer Jahreszahl (JJJJ))', monat  SMALLINT COMMENT 'id of Beitragsvektor (Angabe eines Monats)', zb  DECIMAL(2,2) COMMENT 'Der je Ratenzahlungsabschnitt (tariflich) faellige Zahlbeitrag. Eine Beitragsreduktion aus', tag  SMALLINT COMMENT 'Tag im Monat', btgtyp  INTEGER COMMENT 'id of Beitragsvektor (Beitragstyp (Laufender Beitrag, Zuzahlung, Zulage))', partkey  SMALLINT COMMENT 'the partition key', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbidabg, btgtyp, jahr, lvid, monat, vtid) disable novalidate ) COMMENT 'beitragsvektor' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;