CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (kinderzulage_sid  STRING COMMENT 'Surrgate Key', kinderzulage_id  STRING COMMENT 'Foreign Key Key table', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbnw_sid  STRING COMMENT 'Foreign Key', hvgruppe_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', hvgruppeid  SMALLINT COMMENT 'id of HvGruppe (generically derived from HvGruppe)', bearbnw_id  STRING COMMENT 'Foreign Key', hvgruppe_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', bearbid  INTEGER COMMENT 'the historical version number (logId)', kindid  SMALLINT COMMENT 'id of Kinderzulage (Identifizierung des Kindes)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', geburtsjahr  SMALLINT COMMENT 'Geburtsjahr des Kindes', jahrletzezulage  SMALLINT COMMENT 'Jahr der letzten Zulage', zuordnung  INTEGER COMMENT 'Kennzeichnung, ob die Kinderzulage dem Vertrag des VN oder einem Vertrag des Ehegatten gut', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbidabg, hvgruppeid, kindid, lvid) disable novalidate ) COMMENT 'Daten zur Riester-Kinderzulage' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;