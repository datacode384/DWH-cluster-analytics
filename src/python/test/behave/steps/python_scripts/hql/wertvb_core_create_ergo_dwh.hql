CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (wertvb_sid  STRING COMMENT 'Surrgate Key', wertvb_id  STRING COMMENT 'Foreign Key Key table', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', vb_sid  STRING COMMENT 'Foreign Key', vt_sid  STRING COMMENT 'Foreign Key', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', vb_id  STRING COMMENT 'Foreign Key', vt_id  STRING COMMENT 'Foreign Key', bearbid  INTEGER COMMENT 'the historical version number (logId)', vbid  SMALLINT COMMENT 'id of Vb (generically derived from Vb)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', anrrkw  DECIMAL(2,2) COMMENT 'Anrechenbarer Wert Rueckkauf', anrbfr  DECIMAL(2,2) COMMENT 'Anrechenbarer Wert fuer die Beitragsfreistellung', gbanrwert  DECIMAL(2,2) COMMENT 'Anrechenbarer Wert fuer den Garantiereservebeitrag', tbanrwert  DECIMAL(2,2) COMMENT 'Anrechenbarer Wert fuer den Tarifbeitrag', nbanrwert  DECIMAL(2,2) COMMENT 'Anrechenbarer Wert fuer den Normbeitrag', sgafonds  DECIMAL(2,2) COMMENT 'Schlussueberschussfonds', tbbtgsum  DECIMAL(2,2) COMMENT 'bisherige Tarifbeitragsumme', zbbtgsum  DECIMAL(2,2) COMMENT 'bisherige Zahlbeitragsumme', pbbtgsum  DECIMAL(2,2) COMMENT 'biherige Provisionsbeitragsumme', akztbfin  DECIMAL(2,2) COMMENT 'finanzierte gezillmerte Abschlusskosten fuer den Tarifbeitrag', aknztbfin  DECIMAL(2,2) COMMENT 'finanzierte nicht-gezillmerte Abschlusskosten fuer den Tarifbeitrag', akztbalt  DECIMAL(2,2) COMMENT 'alte gezillmerte Abschlusskosten fuer den Tarifbeitrag', aknztbalt  DECIMAL(2,2) COMMENT 'alte nicht-gezillmerte Abschlusskosten fuer den Tarifbeitrag', akznbfin  DECIMAL(2,2) COMMENT 'finanzierte gezillmerte Abschlusskosten fuer den Normbeitrag', aknznbfin  DECIMAL(2,2) COMMENT 'finanzierte nicht-gezillmerte Abschlusskosten fuer den Normbeitrag', akznbalt  DECIMAL(2,2) COMMENT 'alte gezillmerte Abschlusskosten fuer den Normbeitrag', aknznbalt  DECIMAL(2,2) COMMENT 'alte nicht-gezillmerte Abschlusskosten fuer den Normbeitrag', lakor  DECIMAL(2,2) COMMENT 'Notwendig bei technischen aenderungen von Rueckgewaehrtarifen', basres  DECIMAL(2,2) COMMENT 'Basisreserve', tarres  DECIMAL(2,2) COMMENT 'Tarifreserve', nrmres  DECIMAL(2,2) COMMENT 'Normreserve', garres  DECIMAL(2,2) COMMENT 'Garantiereserve', inkakor  DECIMAL(2,2) COMMENT 'inkaKor', ratzuakor  DECIMAL(2,2) COMMENT 'ratzuaKor', vstkkkor  DECIMAL(2,2) COMMENT 'vstkkKor', vtsaminkkor  DECIMAL(2,2) COMMENT 'vtsaminkKor', bewprovsum  DECIMAL(2,2) COMMENT 'bisherige Provisions-Bewertungssumme', dauerbeitrmrest  INTEGER COMMENT 'restliche Beitragszahlungsdauer in Monaten', minbtgsumakt  DECIMAL(2,2) COMMENT 'Das Attribut enthaelt die vereinbarte Regelbeitragssumme bis zum Berechnungszeitpunkt. Auf', bildr  DECIMAL(2,2) COMMENT 'BilDR als Basis fuer Stichtagsbewegungen', akkalkvol  DECIMAL(2,2) COMMENT 'Kalkulatorisches Abschlusskostenvolumen bei kontenbasierten Tarifen', akbilvol  DECIMAL(2,2) COMMENT 'Bilanzielles Abschlusskostenvolumen bei kontenbasierten Tarifen', akkalkfin  DECIMAL(2,2) COMMENT 'finanzierte kalkulatorische Abschlusskosten bei kontenbasierten Tarifen', akbilfin  DECIMAL(2,2) COMMENT 'Finanzierte bilanzielle Abschlusskosten bei kontenbasierten Tarifen', deltabildr  DECIMAL(2,2) COMMENT 'deltaBilDR als Basis zur Berechnung des Auffuellbedarfs', ratzuavwkkor  DECIMAL(2,2) COMMENT 'Korrektur des Ratenzuschlags fuer Verwaltungskosten bei technischen Aenderungen', ratzuazakor  DECIMAL(2,2) COMMENT 'Korrektur des Ratenzuschlags fuer Zinsausfall bei technischen Aenderungen', ratzuaritodkor  DECIMAL(2,2) COMMENT 'Korrektur des Ratenzuschlags fuer Risiko Tod bei technischen Aenderungen', ratzuarisonkor  DECIMAL(2,2) COMMENT 'Korrektur des Ratenzuschlags fuer sonstige Risiken bei technischen Aenderungen', akmrkwvol  DECIMAL(2,2) COMMENT 'Abschlusskostenvolumen fuer Garantiereserve als Bezugsgroesse fuer Mindestrueckkaufswerte', akmrkwfin  DECIMAL(2,2) COMMENT 'Finanzierte Abschlusskosten fuer Garantiereserve als Bezugsgroesse fuer Mindestrueckkaufsw', partkey  SMALLINT COMMENT 'the partition key', zillres  DECIMAL(2,2) COMMENT 'gezillmerte Nettoreserve (Zillmerreserve)', nettores  DECIMAL(2,2) COMMENT '(ungezillmerte) Nettoreserve', zillbanrwert  DECIMAL(2,2) COMMENT 'Anrechenbarer Wert fuer den Zillmerreservebeitrag', nettobanrwert  DECIMAL(2,2) COMMENT 'Anrechenbarer Wert fuer den Nettoreservebeitrag', aktanspr  DECIMAL(2,2) COMMENT 'aktivierte Ansprueche als Werststand zur Bestimmung des Abschlusskostenverlustes', aktansprkoba  DECIMAL(2,2) COMMENT 'aktivierte Ansprueche aus Zillmerung des kontenbasierten Anteils eines Tarifes als Werstst', fvm  DECIMAL(2,2) COMMENT 'Fondsvermoegen fuer sonstige versicherungstechnische Rueckstellung', drfvm  DECIMAL(2,2) COMMENT 'Fondsvermoegen fuer Deckungsrueckstellung', anrbfrkonten  DECIMAL(2,2) COMMENT 'Anrechenbarer Wert fuer die Beitragsfreistellung aus kontenbasiertem Vermoegen', stoabbfrkonten  DECIMAL(2,2) COMMENT 'Stornoabschlag bei Beitragsfreistellung zur Entnahme aus kontenbasiertem Vermoegen', stoabdkbezrestalt  DECIMAL(2,2) COMMENT 'Bezugsgroesse fuer die Berechnung von Stornoabschlag auf DK vor TAE', stoabfvmbezrestalt  DECIMAL(2,2) COMMENT 'Bezugsgroesse fuer die Berechnung von Stornoabschlag auf FVM vor TAE', stoabakdbezrestalt  DECIMAL(2,2) COMMENT 'Bezugsgroesse fuer die Berechnung von Stornoabschlag auf klassichen Abschlusskosten vor TA', stoabakfbezrestalt  DECIMAL(2,2) COMMENT 'Bezugsgroesse fuer die Berechnung von Stornoabschlag auf kontenbasierten Abschlusskosten v', stoabdkbfr  DECIMAL(2,2) COMMENT 'Stornoabschlag auf DK bei der Beitragsfreistellung', stoabfvmbfr  DECIMAL(2,2) COMMENT 'Stornoabschlag auf Fvm bei der Beitragsfreistellung', steuwertstand  DECIMAL(2,2) COMMENT 'gesamter steuerlicher Wertstand aus KESt-Sicht', auffllerlkl  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ Erleben, klassisch', auffllzinskl  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ Zins, klassisch', auffllbukl  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ BU, klassisch', auffllerldrfvm  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ Erleben, Fondsvermoegen', auffllzinsdrfvm  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ Zins, Fondsvermoegen', auffllzinsva  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ Zins, Verzinsliche Ansammlung', auffllraa  DECIMAL(2,2) COMMENT 'Reduktion aktivierbarer Ansprueche durch Auffuellung', bezrefires  DECIMAL(2,2) COMMENT 'Reserve zur konstruktiven Neuberechnung der Leistung aus Kontostand BEZREFI_LA1', auffllgdr  DECIMAL(2,2) COMMENT 'Differenz zwischen Reserve incl. Zusatzreserve (Bilanzdeckungsrueckstellung nach den neuen', auffllgaa  DECIMAL(2,2) COMMENT 'Differenz zwischen aktivierbaren Anspruechen nach den alten und den neuen Rechnungsgrundla', akztbfinbfr  DECIMAL(2,2) COMMENT 'finanzierte gezillmerte Abschlusskosten fuer den Tarifbeitrag bei Beitragsfreistellung', vwkres  DECIMAL(2,2) COMMENT 'Verwaltungskostenreserve', minantfonds  DECIMAL(2,2) COMMENT 'Mindestanteil zu Ueberschuessen aus Bewertungsreserven', auffllbilrz  DECIMAL(2,2) COMMENT 'Auffuellung fuer Nachreservierungstyp BILRZ', auffllbilrzkb  DECIMAL(2,2) COMMENT 'Auffuellung fuer Nachreservierungstyp BILRZ kontenbasiert', stoabrkwdk  DECIMAL(2,2) COMMENT 'Stornoabschlag auf Deckungskapital bei Rueckkauf im Rahmen einer Beitragsfreistellung', stoabrkwkonten  DECIMAL(2,2) COMMENT 'Stornoabschlag auf kontenbasiertes Vermoegen bei Rueckkauf im Rahmen einer Beitragsfreiste', auffllzinsvaren  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ Zins, Verzinsliche Ansammlung, aus Rentenbezugszeit herruehrend', auffllerlva  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ Erleben, Verzinsliche Ansammlung', auffllerlvaren  DECIMAL(2,2) COMMENT 'Auffuellung bei Typ Erleben, Verzinsliche Ansammlung, aus Rentenbezugszeit herruehrend', akzkalkbisher  DECIMAL(2,2) COMMENT 'Bisheriger Anteil der klassischen gezillmerten kalkulatorischen AK', aknzkalkbisher  DECIMAL(2,2) COMMENT 'Bisheriger Anteil der klassischen nicht gezillmerten kalkulatorischen AK', akzbilbisher  DECIMAL(2,2) COMMENT 'Bisheriger Anteil der klassischen gezillmerten bilanziellen AK', aknzbilbisher  DECIMAL(2,2) COMMENT 'Bisheriger Anteil der klassischen nicht gezillmerten bilanziellen AK', akmrkwbisher  DECIMAL(2,2) COMMENT 'Bisheriger Anteil der AK fuer Mindestrueckkaufswert', akkalkvolbisher  DECIMAL(2,2) COMMENT 'Bisheriger Anteil des kalkulatorischen Abschlusskostenvolumens bei kontenbasierten Tarifen', akbilvolbisher  DECIMAL(2,2) COMMENT 'Bisheriger Anteil des bilanziellen Abschlusskostenvolumens bei kontenbasierten Tarifen', akkalkfondsfin  DECIMAL(2,2) COMMENT 'Finanzierte fondsspezifische kalkulatorische Abschlusskosten, falls Abschlusskosten auf ei', akkalkfondsvol  DECIMAL(2,2) COMMENT 'Fondsspezifisches kalkulatorisches Abschlusskostenvolumen bei kontenbasierten Tarifen, fal', steuzuschlaggezsum  DECIMAL(2,2) COMMENT 'Summe der bisher gezahlten Steuer auf Praemien des Vertragsbausteins', auffllraaerl  DECIMAL(2,2) COMMENT 'Reduktion aktivierbarer Ansprueche durch Auffuellung bei Nachreservierung Erleben', auffllraazins  DECIMAL(2,2) COMMENT 'Reduktion aktivierbarer Ansprueche durch Auffuellung bei Nachreservierung Zins (Zinszusatz', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbidabg, lvid, vbid, vtid) disable novalidate ) COMMENT 'Wertstandsgroessen auf VB - EbeneWertstand-VB: Wertstandsgroessen auf VB - EbeneHistorie-streng: Attribute, die zur strengen Historienfuehrung am Vertrag benoetigt werden' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;