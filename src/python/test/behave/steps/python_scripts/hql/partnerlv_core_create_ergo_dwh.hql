CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (partnerlv_sid  STRING COMMENT 'Surrgate Key', partnerlv_id  STRING COMMENT 'Foreign Key Key table', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', partnerid  INTEGER COMMENT 'id of PartnerLV (Das Field partnerId identifiziert denPartner, zu dem diese Row eine Aussa', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', rollenid  INTEGER COMMENT 'id of PartnerLV (Identifikation einer Partnerrolle in einer Partner-Vertrags-Beziehung)', bearbid  INTEGER COMMENT 'the historical version number (logId)', kontoid  INTEGER COMMENT 'ID der zugeordneten Bankverbindung zum Partner', kzhauptvn  INTEGER COMMENT 'Dieses Kennzeichen gibt an, ob der jeweilige Versicherungsnehmer der sog. ""Haupt-VN"" ist', anschriftid  INTEGER COMMENT 'ID der zugeordneten Anschrift zum Partner', gebdat  TIMESTAMP COMMENT 'Das Geburtsdatum ist der Termin, an dem die natuerliche Person geboren wurde.', landid  INTEGER COMMENT 'Wohnsitzland des Partners. Schluessel fuer den in definierenden Daten hinterlegten Wert fu', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', kzpartnerart  INTEGER COMMENT 'Kennzeichnung, ob es sich um eine juristische oder natuerliche Person', kistaminfodat  TIMESTAMP COMMENT 'Datum eines Hinweisschreiben fuer das Widerspruchsrecht bzgl. der KiStAM-Abfrage', risikoprofil  INTEGER COMMENT 'Erfahrung des Partners bei der Anlage in fondsgebundene Produkte.', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbidabg, lvid, partnerid, rollenid) disable novalidate ) COMMENT 'Partner werden einzelnen Vertraegen bzw. Vertragsteilen zugeordnet. Dabei wird auch festgehalten, welche Rolle er spielt.Beitragszahler: Der Beitragszahler ist ein Partner zum LV-Vertrag, der die Beitraege bezahlt. Der Beitragszahler kann vom Versicherun' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;