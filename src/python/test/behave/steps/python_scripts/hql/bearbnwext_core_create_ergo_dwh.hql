CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (bearbnwext_sid  STRING COMMENT 'Surrgate Key', bearbnwext_id  STRING COMMENT 'Foreign Key Key table', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbid  INTEGER COMMENT 'the historical version number (logId)', kzprovstorno  INTEGER COMMENT 'Kennzeichen, ob ein Vermittler seine aufgrund dieser Bearbeitung erhaltene Provision im Fa', meldedat  TIMESTAMP COMMENT 'Datum, zu dem eine Meldung eingeht.', bearbidref  INTEGER COMMENT 'Das Field bearbIdRef identifiziert zusammen mit dem Field lvId den Vertragsstand, von dem ', spmig  DECIMAL(2,2) COMMENT 'Der Sparbeitrag, der aus einer Migration stammt', vorglstnetto  DECIMAL(2,2) COMMENT 'Vorgegebene Nettoleistung nach Abzug der Steuer.', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbid, lvid) disable novalidate ) COMMENT 'bearbNwExt' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;