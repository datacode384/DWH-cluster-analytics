CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (vtlstg_sid  STRING COMMENT 'Surrgate Key', vtlstg_id  STRING COMMENT 'Foreign Key Key table', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', vt_sid  STRING COMMENT 'Foreign Key', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', vt_id  STRING COMMENT 'Foreign Key', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', lstgtyp  INTEGER COMMENT 'id of VtLstg (Leistungsart)', partkey  SMALLINT COMMENT 'the partition key', bearbid  INTEGER COMMENT 'the historical version number (logId)', lstgwertakt  DECIMAL(4,4) COMMENT 'Aktueller Leistungswert', lstgwertrel  DECIMAL(8,8) COMMENT 'Relativer Leistungswert', vorgtyp  INTEGER COMMENT 'Steuert, ob die Leistung relativ oder absolut vorgegeben wird', lstgprofil  INTEGER COMMENT 'Leistungsprofil fuer die aktuelle Leistungsart', lstgoption  INTEGER COMMENT 'Das Attribut steuert, wie die Leistungen beim ausserplanmaessigen Leistungsuebergang angep', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbidabg, lstgtyp, lvid, vtid) disable novalidate ) COMMENT 'Speicherung von Leistungen und Leistungsvorgabe je Leistungsart am Vertragsteil' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;