CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', zutsys  INTEGER COMMENT 'id of SkUebZu (Systematik fuer die ueberschuss - Zuteilung: nachschuessig, vorschuessig ra', anzir  DECIMAL(2,2) COMMENT 'Absolutbetrag der Zinsen auf das verzinsliche Ansammlungsguthaben', anzidir  DECIMAL(2,2) COMMENT 'Zinsen auf das Ansammlungsguthaben aus Direktgutschrift', anzirfb  DECIMAL(2,2) COMMENT 'Zinsen auf das Ansammlungsguthaben aus der RfB', risdir  DECIMAL(2,2) COMMENT 'Zugeteilter Risikoueberschuss aus der Direktgutschrift.', risrfb  DECIMAL(2,2) COMMENT 'Zugeteilter Risikoueberschuss aus der RfB.', grdir  DECIMAL(2,2) COMMENT 'Zugeteilter Grundueberschuss aus der Direktgutschrift.', grrfb  DECIMAL(2,2) COMMENT 'Zugeteilter Grundueberschuss aus der RfB.', zidir  DECIMAL(2,2) COMMENT 'Zugeteilter Zinsueberschuss aus der Direktgutschrift.', zirfb  DECIMAL(2,2) COMMENT 'Zugeteilter Zinsueberschuss aus der RfB.', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', anzirfbbed  DECIMAL(2,2) COMMENT 'Bedingte Zinsen auf das Ansammlungsguthaben aus der RfB', anzidirbed  DECIMAL(2,2) COMMENT 'Bedingte Zinsen auf das Ansammlungsguthaben aus der Direktgutschrift', refizidir  DECIMAL(2,2) COMMENT 'Zinsen auf das Refinanzierungskonto aus der Direktgutschrift', refizirfb  DECIMAL(2,2) COMMENT 'Zinsen auf das Refinanzierungskonto aus der RfB', partkey  SMALLINT COMMENT 'the partition key', vbid  SMALLINT COMMENT 'id of Vb (generically derived from Vb)', bewresdir  DECIMAL(2,2) COMMENT 'Zugeteilter Ueberschuss fuer Bewertungsreserven aus Direktgutschrift', bewresrfb  DECIMAL(2,2) COMMENT 'Zugeteilter Ueberschuss fuer Bewertungsreserven aus RfB', bewresmindir  DECIMAL(2,2) COMMENT 'Zugeteilter Ueberschuss fuer Mindestbeteiligung an Bewertungsreserven aus Direktgutschrift', bewresminrfb  DECIMAL(2,2) COMMENT 'Zugeteilter Ueberschuss fuer Mindestbeteiligung an Bewertungsreserven aus Rfb', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'Sammelkontostand ueberschusszuteilung Kontostaende fuer die Fuehrung der zugeteilten, aber noch nicht verwendeten ueberschussanteile.Gruppierungsmerkmale: Fuer die Rechnungslegung relevante Gruppierungsmerkmale.Historie-streng: Attribute, die zur strenge' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;