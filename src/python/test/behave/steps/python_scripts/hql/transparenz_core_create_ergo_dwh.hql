CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (transparenz_sid  STRING COMMENT 'Surrgate Key', transparenz_id  STRING COMMENT 'Foreign Key Key table', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', vt_sid  STRING COMMENT 'Foreign Key', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', vt_id  STRING COMMENT 'Foreign Key', bearbid  INTEGER COMMENT 'the historical version number (logId)', infotyp  INTEGER COMMENT 'id of Transparenz (Art einer Information)', infobetrag  DECIMAL(5,5) COMMENT 'Information in Form einer Zahl', infobezug  INTEGER COMMENT 'Bezugsgroesse, auf die sich die ermittelten Werte zu dem Typ beziehen', varianteid  STRING COMMENT 'id of Transparenz (Attribut zur eindeutigen Identifizierung einer Tarifvariante (wie in sb', gueab  TIMESTAMP COMMENT 'Gueltig ab, Beginntermin der Zuordnung, Zuordnung ist zum Beginntermin und ab dem Beginnte', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbidabg, infotyp, lvid, varianteid, vtid) disable novalidate ) COMMENT 'Eine Zeile in der Tabelle ""transparenz"" repraesentiert eine Information, die an den Versicherungsnehmer weitergegeben werden muss.' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;