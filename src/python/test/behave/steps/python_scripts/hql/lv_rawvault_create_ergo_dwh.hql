--usage: hive --hiveconf database=rawvault_bddtest --hiveconf table_name=lv -f lv_rawvault_create_ergo_dwh.hql

CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (LVID STRING ,
		PDID STRING ,
		GENERATION STRING ,
		MANDANTID INTEGER ,
		VERZINSBEGINN TIMESTAMP ,
		ZWEIINK INTEGER ,
		ZINSBDEP DECIMAL(16,
		8) ,
		BEARBID INTEGER ,
		BEARBIDABG INTEGER ,
		GUEBISZINS TIMESTAMP ,
		BETRAG DECIMAL(16,
		2) ,
		WAEHRUNGID INTEGER ,
		LVBEGT TIMESTAMP ,
		LVABLT TIMESTAMP ,
		TRDK DECIMAL(16,
		2) ,
		BTRDIFFMIG DECIMAL(16,
		2) ,
		BTRABGL DECIMAL(16,
		2) ,
		VSTKK DECIMAL(16,
		2) ,
		UEBVERR DECIMAL(16,
		2) ,
		LVJAHRTAG TIMESTAMP ,
		LVSTATUS INTEGER ,
		ZB DECIMAL(16,
		2) ,
		VZBORIG DECIMAL(16,
		2) ,
		KZVORG INTEGER ,
		KZ_ANR INTEGER ,
		ANREWERT DECIMAL(16,
		2) ,
		ANNAHMEART INTEGER ,
		FR_SWITCH INTEGER ,
		LET_SWITCH TIMESTAMP ,
		KZ_VERSST INTEGER ,
		VERSST DECIMAL(16,
		2) ,
		RENTBEKZ INTEGER ,
		VERTRIEBSWEG INTEGER ,
		VERSSTNATE DECIMAL(16,
		2) ,
		ERRBTGNIV DECIMAL(16,
		2) ,
		MINBTG DECIMAL(16,
		2) ,
		ZWEIVORZA INTEGER ,
		BTRDIFFWSW DECIMAL(16,
		2) ,
		KZNACHZ INTEGER ,
		UEBVERRNSP DECIMAL(16,
		2) ,
		KZANBIETERWECHSEL INTEGER ,
		KZRELEASE INTEGER ,
		ANTRAGSSTEUERUNG INTEGER ,
		PARTKEY INTEGER ,
		KZANGVERS INTEGER ,
		KZAUSZSTOP INTEGER ,
		KZABFINDUNGKBR INTEGER ,
		KOLLNR STRING ,
		LFDKOLLNR STRING ,
		VERWGRPNR STRING ,
		MUSTERID STRING ,
		ZUZAHLPOL DECIMAL(16,
		2) ,
		ISVORAUSSSCHAEDVERW INTEGER ,
		KISTAMWIRKDAT TIMESTAMP ,
		KISTAMBEARBDAT TIMESTAMP ,
		KZLEISTUNGSARTTOD INTEGER ,
		STEULANDSEKUNDAER INTEGER ,
		STEUZUSCHLAGZUZAHL DECIMAL(15,
		2) ,
		STEUZUSCHLAG DECIMAL(15,
		2) ,
		STEULANDPRIMAER INTEGER ,
		KZRECHENKERN INTEGER ,
		VORGPERFORMANCE DECIMAL(15,
		8) ,
		C_WERBHILF INTEGER ,
		C_ZUGWEG INTEGER ,
		C_WIKMGL INTEGER, insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'Die Rows in Table lv repraesentieren einen oder mehrere Vertragsstaende; temporal identifiziert durch das Field lvId. Die Vertragsstaende sind von 1 beginnend durchnummeriert, wobei es auch Luecken geben darf. Welche Vertragsstaende eine Row repraesentie' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;

