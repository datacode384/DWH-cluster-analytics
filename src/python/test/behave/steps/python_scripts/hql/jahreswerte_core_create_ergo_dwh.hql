CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (jahreswerte_sid  STRING COMMENT 'Surrgate Key', jahreswerte_id  STRING COMMENT 'Foreign Key Key table', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', vt_sid  STRING COMMENT 'Foreign Key', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', vt_id  STRING COMMENT 'Foreign Key', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', jahr  SMALLINT COMMENT 'id of Jahreswerte (Angabe einer Jahreszahl (JJJJ))', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', btgsumakt  DECIMAL(2,2) COMMENT 'Aktuelle Summe der im Bezugsjahr gezahlten Beitraege, aufgeteilt auf die betroffenen Vertr', btguebjahr  DECIMAL(2,2) COMMENT 'Anteil an der Beitragssumme des aktuellen Kalenderjahres, der nicht im Deckungskapital zum', zulsumakt  DECIMAL(2,2) COMMENT 'Aktuelle Summe der im Bezugsjahr gezahlten Zulagen, aufgeteilt auf die betroffenen Vertrag', rbsum  DECIMAL(2,2) COMMENT 'Summe Risikobeitrag', sbsum  DECIMAL(2,2) COMMENT 'Summe Spar- bzw. Anlagebeitrag', vwsum  DECIMAL(2,2) COMMENT 'Summe Verwaltungskosten', partkey  SMALLINT COMMENT 'the partition key', entspsum  DECIMAL(2,2) COMMENT 'Summe Entsparbetrag', zulkorrakt  DECIMAL(2,2) COMMENT 'Aktuelle Summe der im Bezugsjahr erfolgten Zulagenkorrekturen', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbidabg, jahr, lvid, vtid) disable novalidate ) COMMENT 'In Tabelle jahreswerte werden fuer jedes Kalenderjahr Vertragsgroessen als Summenwerte analog zu einem Sammelkonto festgehalten. Es wird jeweils der aktuelle Stand geflossener Zahlungsstroeme und der Beitragsaufteilung in Risiko-/Spar-/Kostenteil gefuehr' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;