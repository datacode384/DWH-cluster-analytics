CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (poldarl_sid  STRING COMMENT 'Surrgate Key', poldarl_id  STRING COMMENT 'Foreign Key Key table', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', mandantid  INTEGER COMMENT 'Das Field mandantId enthaelt einen Schluesselwert fuer den Mandant, fuer den diese Row gue', zahlwegpoldarl  INTEGER COMMENT 'Zahlweg Policendarlehen', zahlwegzins  INTEGER COMMENT 'Zahlweg Zinszahler Policendarlehen', zahlwegrueckz  INTEGER COMMENT 'Zahlweg fuer die Rueckzahlung des Policendarlehens', zinssatz  DECIMAL(3,3) COMMENT 'Prozentsatz; wird bestimmt auf globpara', abwzinssatz  DECIMAL(3,3) COMMENT 'Abweichender Zins (Prozentsatz)', zinsab  TIMESTAMP COMMENT 'Zeitpunkt ab dem das Policendarlehen zu verzinsen ist', zweizins  INTEGER COMMENT 'Vereinbarte Zahlweise der Zinsen fuer das Policendarlehen', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbid, lvid) disable novalidate ) COMMENT 'Policendarlehen' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;