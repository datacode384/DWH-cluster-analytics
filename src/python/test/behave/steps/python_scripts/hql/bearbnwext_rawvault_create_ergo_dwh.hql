CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbid  INTEGER COMMENT 'the historical version number (logId)', kzprovstorno  INTEGER COMMENT 'Kennzeichen, ob ein Vermittler seine aufgrund dieser Bearbeitung erhaltene Provision im Fa', meldedat  TIMESTAMP COMMENT 'Datum, zu dem eine Meldung eingeht.', bearbidref  INTEGER COMMENT 'Das Field bearbIdRef identifiziert zusammen mit dem Field lvId den Vertragsstand, von dem ', spmig  DECIMAL(2,2) COMMENT 'Der Sparbeitrag, der aus einer Migration stammt', vorglstnetto  DECIMAL(2,2) COMMENT 'Vorgegebene Nettoleistung nach Abzug der Steuer.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'bearbNwExt' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;