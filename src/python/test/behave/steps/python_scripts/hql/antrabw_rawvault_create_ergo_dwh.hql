CREATE TABLE IF NOT EXISTS ${hiveconf:database}.${hiveconf:table_name}  (antrabwid  INTEGER COMMENT 'id of AntrAbw (Intern vergebene Nummer, der die Antragsabweichung innerhalb der LV identif', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', antrabwart  INTEGER COMMENT 'Art der Abweichung vom Antrag', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.')  COMMENT 'antrAbw' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;