CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (sbuebverw_sid  STRING COMMENT 'Surrgate Key', sbuebverw_id  STRING COMMENT 'Foreign Key Key table', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', vt_sid  STRING COMMENT 'Foreign Key', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', vt_id  STRING COMMENT 'Foreign Key', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbid  INTEGER COMMENT 'the historical version number (logId)', wirkbeginn  TIMESTAMP COMMENT 'Termin, ab dem der neue Vertragsstand fachlich wirksam ist.', feinbeginn  SMALLINT COMMENT 'Feinsteuerungsinformation zum Termin Termin-Wirksamkeit-Beginn', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', sbid  INTEGER COMMENT 'id of SbUebVerw (Eindeutige Kennzeichnung einer Sammelbewegung)', vsys  INTEGER COMMENT 'id of SbUebVerw (Systematik fuer die ueberschuss - Verwendung: ratierlich oder Stichtag)', standid  INTEGER COMMENT 'Zuordnung einer Bewegung zu einem (historischen) Vertragsstand', wirkende  TIMESTAMP COMMENT 'Termin, ab dem der Vertragsstand fachlich nicht mehr wirksam ist.', bwggrdid  INTEGER COMMENT 'Eindeutige Identifikation der Ursache fuer bestimmte Kontobewegungen, die in Form einer Sa', bearbidsto  INTEGER COMMENT 'Wird eine Bearbeitung storniert, enthaelt die bearbIdSto des aktuellen Vorgangs die bearbI', feinende  SMALLINT COMMENT 'Feinsteuerungsinformation zum Termin Termin-Wirksamkeit-Ende', vva  DECIMAL(2,2) COMMENT 'ueberschussanteil, der in die verzinsliche Ansammlung fliesst.', bearbdat  TIMESTAMP COMMENT 'Termin, an dem das Objekt bearbeitet wurde.', vbon  DECIMAL(2,2) COMMENT 'ueberschussanteil, der in den Bonus fliesst.', vbarr  DECIMAL(2,2) COMMENT 'ueberschussanteil, der bar ausgezahlt wird.', vver  DECIMAL(2,2) COMMENT 'ueberschussanteil, der fuer die Beitragsverrechnung verwendet wird.', vtodbon  DECIMAL(2,2) COMMENT 'ueberschussanteil, der fuer den Todesfall-Sofortbonus verwendet wird.', anteilausrfb  DECIMAL(8,8) COMMENT 'Aus der RfB stammender Anteil pro ueberschussverwendung (der Rest stammt aus Direktgutschr', vfonds  DECIMAL(2,2) COMMENT 'Aufwand Fondsanteile - ueberschussanteil, der in Fondsanteilen angelegt wird.', vsofortbon  DECIMAL(2,2) COMMENT 'Aufwand-Sofortbonus (nicht Todesfall) - ueberschussanteil, der fuer den Sofortbonus verwen', pdid  STRING COMMENT 'Eindeutige Identifikation des Produkts, das dem LV-Vertrag zugeordnet ist.', tfid  STRING COMMENT 'Eindeutiger Identifikator eines LV-Tarifs', sex  INTEGER COMMENT 'Identifizierender Schluessel fuer das Geschlecht einer natuerlichen Person.', kzeinmalbtrg  INTEGER COMMENT 'Kennzeichen, ob Einmalbeitrag vorliegt oder nicht.', kzkoll  SMALLINT COMMENT 'Ordnet eine Produkt-/Tarif-Kombination dem Kollektiv- oder Einzelbereich zu', waehrungid  INTEGER COMMENT 'Das Attribut dient zur eindeutigen Identifizierung einer Waehrung', mandantid  INTEGER COMMENT 'Das Field mandantId enthaelt einen Schluesselwert fuer den Mandant, fuer den diese Row gue', konsortid  INTEGER COMMENT 'Das Field konsortId identifiziert die Konsortialvereinbarung, nach der dieser Vertragsteil', zustand  INTEGER COMMENT 'In diesem Attribut wird der aktuelle Status des Vertragsbausteins verschluesselt.', vfondsbil  DECIMAL(2,2) COMMENT 'Aufwand Fondsanteile - ueberschussanteil, angefallen an einer Zusatzversicherung ohne eige', varianteid  STRING COMMENT 'id of SbUebVerw (Attribut zur eindeutigen Identifizierung einer Tarifvariante.)', partkey  SMALLINT COMMENT 'the partition key', zeichjahr  TIMESTAMP COMMENT 'Zeichnungsjahr,Gruppierungsmerkmal', bewresantausrfb  DECIMAL(8,8) COMMENT 'Fuer Ueberschuesse aus Bewertungsreserven: Aus Rfb stammender Anteil pro Ueberschussverwen', bewresvbar  DECIMAL(2,2) COMMENT 'Ueberschuesse aus Bewertungsreserven als Barauszahlung (kein davon-Wert, zusaetzlich zu vb', bewresvbon  DECIMAL(2,2) COMMENT 'Ueberschuesse aus Bewertungsreserven zur Erhoehung der Bonusleistung (kein davon-Wert, zus', uebtransferabgrfb  DECIMAL(2,2) COMMENT 'Betrag der lfd. Ueberschuesse aus RfB, der von einem Vertragsteil an einen oder mehrere an', uebtransferabgdg  DECIMAL(2,2) COMMENT 'Betrag der lfd. Ueberschuesse aus Dg, der von einem Vertragsteil an einen oder mehrere and', uebtransferaufnrfb  DECIMAL(2,2) COMMENT 'Betrag der lfd. Ueberschuesse aus RfB, der von einem oder mehreren Vertragsteilen an diese', uebtransferaufndg  DECIMAL(2,2) COMMENT 'Betrag der lfd. Ueberschuesse aus Dg, der von einem oder mehreren Vertragsteilen an diesen', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbid, lvid, sbid, varianteid, vsys, vtid) disable novalidate ) COMMENT 'Sammelbewegung ueberschussverwendung Sammelbewegung zur Dokumentation des verwendeten ueberschusses, aufgegliedert auf die einzelnen Verwendungsarten.Gruppierungsmerkmale: Fuer die Rechnungslegung relevante Gruppierungsmerkmale.SB-Ueberschussverwendung: ' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;