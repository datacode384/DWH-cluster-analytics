CREATE TABLE IF NOT EXISTS  ${hiveconf:database}.${hiveconf:table_name}  (eaklausel_sid  STRING COMMENT 'Surrgate Key', eaklausel_id  STRING COMMENT 'Foreign Key Key table', bearbnw_sid  STRING COMMENT 'Foreign Key', lv_sid  STRING COMMENT 'Foreign Key', partnerlv_sid  STRING COMMENT 'Foreign Key', vt_sid  STRING COMMENT 'Foreign Key', bearbnw_id  STRING COMMENT 'Foreign Key', lv_id  STRING COMMENT 'Foreign Key', partnerlv_id  STRING COMMENT 'Foreign Key', vt_id  STRING COMMENT 'Foreign Key', partnerid  INTEGER COMMENT 'id of VpVT (Das Field partnerId identifiziert die versicherte Person, zu der diese Row ein', lvid  STRING COMMENT 'id of Lv (Das Field lvId identifiziert denVertrag, zu dem diese Row gehoert.)', bearbid  INTEGER COMMENT 'the historical version number (logId)', bearbidabg  INTEGER COMMENT 'the historical version that invalidates this version (nextLogId)', rollenid  INTEGER COMMENT 'id of VpVT (Identifikation einer Partnerrolle in einer Partner-Vertrags-BeziehungConstrain', vtid  SMALLINT COMMENT 'id of Vt (generically derived from Vt)', lfdnr  INTEGER COMMENT 'id of EaKlausel (In diesem Attribut wird eine ""laufende Nummer"" (natuerliche Zahl) gefue', klauselid  INTEGER COMMENT 'Eindeutige Identifikation einer (verschluesselbaren) Standard-Klausel zur Definition eines', klauseltext  STRING COMMENT 'Genaue Spezifikation einer E/A-Klausel, z.B. Angabe einer Erkrankung oder Funktionseinschr', partkey  SMALLINT COMMENT 'the partition key', diff_hash  STRING COMMENT 'Hash for comparing records for inserts.', record_hash  STRING COMMENT 'Hash for all non technical attributes.', insert_tst  TIMESTAMP COMMENT 'Timestamp for creation of record.', source_system  INTEGER COMMENT 'Name of source system.', primary key(bearbidabg, lfdnr, lvid, partnerid, rollenid, vtid) disable novalidate ) COMMENT 'Ein-/Ausschluss-Klausel: Es gibt Aus- bzw. Einschluesse aufgrund medizinischer, beruflicher und sportlicher Risiken. Einschlussklausel bedeutet, dass ein bestimmtes Risiko explizit eingeschlossen wird. Fuer eine Ausschlussklausel gilt die analoge Aussage' PARTITIONED BY (tenant_name  STRING COMMENT 'Name of the tenant.', process_id  INTEGER COMMENT 'Unique id for ETL-Processes.')  STORED AS parquet;